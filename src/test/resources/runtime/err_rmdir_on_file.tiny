// Expected exit code: 70
import filesystem
// Create a regular file, then try rmdir on it — should fail
filesystem.write("temp_rmdir_file_conflict.txt", "hello")
filesystem.rmdir("temp_rmdir_file_conflict.txt")
