// Expected exit code: 70
// Expected error: Runtime error: Second argument to 'rmdir' must be a boolean (got integer).
import filesystem
// Passing a non-boolean as the second argument to rmdir — should fail
filesystem.mkdir("temp_rmdir_bad_arg")
filesystem.rmdir("temp_rmdir_bad_arg", 42)
